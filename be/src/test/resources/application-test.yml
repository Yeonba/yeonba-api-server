spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:h2:mem:~/yeonba_test_db
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: false
        format_sql: false
